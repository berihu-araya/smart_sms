import PublicInfoPage from "@/components/PublicInfoPage";

export const metadata = { title: "Features | Smart SMS" };

export default function FeaturesPage() {
  return <PublicInfoPage page="features" />;
}